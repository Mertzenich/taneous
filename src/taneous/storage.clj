(ns taneous.storage)

(throw (Exception. "Storage not implemented yet"))

;; (defn get-download-url
;;   [app pathname]
;;   (util/get app
;;              "/admin/storage/signed-download-url"
;;              {:app-id (:app-id app) :filename pathname}))
